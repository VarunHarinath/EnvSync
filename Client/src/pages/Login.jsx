import React,{useState} from "react";import {Navigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";import Input from "../components/common/Input";import Button from "../components/common/Button";import {EnvSyncLogo} from "../components/common/Logo";
export default function Login(){const {user,login}=useAuth(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);if(user)return <Navigate to="/projects" replace/>;const submit=async e=>{e.preventDefault();setBusy(true);setError("");try{await login(email,password)}catch(err){setError(err.message)}finally{setBusy(false)}};return <main className="min-h-screen bg-background grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-sm border bg-card rounded-xl p-8 space-y-5 shadow-sm"><div><EnvSyncLogo markClassName="h-9 w-9" wordmarkClassName="text-xl"/><p className="mt-3 text-sm text-muted-foreground">Sign in to your self-hosted secrets workspace</p></div>{error&&<p role="alert" className="text-sm text-destructive bg-destructive/10 rounded p-3">{error}</p>}<label className="block text-sm font-medium">Email<Input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required className="mt-2"/></label><label className="block text-sm font-medium">Password<Input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required className="mt-2"/></label><Button className="w-full" type="submit" isLoading={busy}>Sign in</Button><p className="text-xs text-muted-foreground">Accounts are provisioned by your EnvSync administrator.</p></form></main>}
